import classes from './HomePage.module.css';

function HomePage() {
    return (
        <header className={classes.image}>
            <div>Login</div>
        </header>
    );
}

export default HomePage;