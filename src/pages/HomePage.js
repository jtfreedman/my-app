import classes from './HomePage.module.css';

function HomePage() {
    return (
        <section>
            <h1 className={classes.h1}>Home</h1>
        </section>
    );
}

export default HomePage;